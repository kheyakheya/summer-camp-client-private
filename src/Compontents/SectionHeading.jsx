
const SectionHeading = ({heading}) => {
    return (
        <div className='flex justify-center align-center text-5xl text-red-700 pt-12'>

            <div>
                 <h2>{heading}</h2>

            </div>

        </div>
    );
};

export default SectionHeading;